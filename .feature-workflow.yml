# Feature workflow configuration
# Written by /feature-init — edit anytime to change settings

branch:
  prefix: "feat/"       # Branch naming: <prefix><feature-id>
  target: "dev"         # Base branch for PRs and merges

reviewer: "none"        # External reviewer: gemini, codex, or none
